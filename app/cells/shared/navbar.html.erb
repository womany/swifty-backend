<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="row">
      <div class="col-sm-3">
        <div class="navbar-header pull-right">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to('Swifty', root_path, :class => 'navbar-brand') %>
        </div>
        <!-- Brand and toggle get grouped for better mobile display -->
      </div>
      <div class="col-sm-6 lighten-header">
        <% if user_signed_in? %>
          <div class="input-group" id="search">
            <span class="input-group-addon" id="search-bar"><i class="fa fa-search fa-fw"></i></span>
            <input class="form-control" id="search-input" type="input" placeholder="搜尋……">
          </div>
        <% end %>
      </div>
      <div class="col-sm-3">
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse pull-left">
          <% if user_signed_in? %>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to('編輯會員資料', edit_user_registration_path) %></li>
                <li><%= link_to('登出', destroy_user_session_path, :method => :delete) %></li>
              </ul>
            </li>
          </ul>
          <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to('登入 / 註冊', new_user_session_path) %></li>
          </ul>
          <% end %>
        </div><!-- /.navbar-collapse -->
      </div>
    </div>
  </div>
</nav>